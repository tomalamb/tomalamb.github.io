# Development override settings
url: http://localhost:4000
baseurl: ""

# Disable minification for faster builds
jekyll-minifier:
  enabled: false

# Disable image processing locally (requires imagemagick)
imagemagick:
  enabled: false

sass:
  style: expanded
